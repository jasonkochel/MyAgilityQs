{
  "name": "@my-agility-qs/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "test": "npm run type-check && echo \"✅ Shared type check passed\""
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "rimraf": "^5.0.7"
  },
  "files": [
    "dist"
  ]
}
